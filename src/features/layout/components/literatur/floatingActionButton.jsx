import React from 'react'; 

const FloatingActionButton = (props) => { 
  
  return ( 
    
    <floatingActionButton>{props.text}</floatingActionButton> 
    
  ); 
  
} 

export {FloatingActionButton};